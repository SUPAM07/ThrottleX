apiVersion: v1
kind: Service
metadata:
  name: throttlex-frontend
  labels:
    app: throttlex-frontend
spec:
  selector:
    app: throttlex-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
