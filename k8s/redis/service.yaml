apiVersion: v1
kind: Service
metadata:
  name: throttlex-redis
  labels:
    app: throttlex-redis
spec:
  selector:
    app: throttlex-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP
